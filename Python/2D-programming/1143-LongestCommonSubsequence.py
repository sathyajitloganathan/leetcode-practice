class Solution:
    def longestCommonSubsequence(self, text1: str, text2: str) -> int:

        space = [0 for i in range(len(text1) + 1) for j in range(len(text2) + 1)]

        for 

        if (len(text1) > len(text2)):
            for i in range(text2):
                
        else:
            text1
        
        
        
        return 1